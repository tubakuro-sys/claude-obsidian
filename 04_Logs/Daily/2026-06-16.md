# 2026-06-16（火）

## 家族共有アプリ（Supabase版）— フェーズ1 環境構築に着手

手順書（`02_Projects/家族共有アプリ/手順書.md`）に沿ってクラウド設定（手順1〜3）を開始。

### 完了したこと
- **手順1：Supabaseプロジェクト作成** ✅
  - プロジェクト名：`family-app`
  - Organization：tubakuro-sys's Org（Free / $0）
  - Region：Northeast Asia (Tokyo) / ap-northeast-1・t4g.nano
  - Security設定：Enable Data API=ON、Automatically expose new tables=ON、Enable automatic RLS=OFF
    - （expose new tablesをONにしたのは、anon key経由でアプリがテーブルを読む設計のため。データ保護はschema.sqlのRLSが担当）
  - Database password：自動生成し控えた（ogawa保管）
  - Project URL / anon key：Supabaseダッシュボードから取得可（実装手順4で使用）
- **手順2：schema.sql 実行** ✅
  - SQL Editorに全文貼り付け→Run→「Success. No rows returned」
  - families / profiles / recipes / recipe_ingredients / recipe_steps / tags / recipe_tags / meal_plans / shopping_list_items / tasks 作成
  - RLS全テーブル有効化＋ポリシー＋profiles自動作成トリガー＋my_family_id()ヘルパーまで反映

### 途中まで（次回ここから）
- **手順3：Google認証＋リダイレクトURL設定**（ブロックC）← 未完了
  - 現在地：Authentication画面まで開いた。「Sign In / Providers」はまだ押していない
  - 次の一手（C-1）：Authentication →「Sign In / Providers」→ Google を開き「Callback URL (for OAuth)」をコピー
    （`https://<プロジェクトID>.supabase.co/auth/v1/callback` 形式）
  - C-2：Google Cloud Console で OAuth認証情報を作成
    - Google Auth Platform →「Get Started」→ App名/メール/User type=External/連絡先
    - OAuth client（Web application）作成 → Authorized redirect URIs に C-1のコールバックURLを貼る
    - Client ID / Client Secret を取得
    - ※家族用なのでAudience画面で家族のGmailを Test users に追加する方針（一般公開＝Google審査を避ける）
  - C-3：取得したClient ID/SecretをSupabaseのGoogleプロバイダに貼って有効化
  - C-4：Authentication →「URL Configuration」で Site URL / Redirect URLs 設定
    - ローカル確認用に `http://localhost:5500` 等を先に追加。GitHub Pages URLは手順6で追加

### メモ・気づき
- 今のSupabase UIではプロバイダ設定は「Authentication →（CONFIGURATION）Sign In / Providers」にある
- Googleの同意画面は新「Google Auth Platform」体験に変わっている（旧OAuth consent screenとは画面構成が違う）
- 混同注意：Google Cloud側に登録するのは「SupabaseのコールバックURL（固定）」、Supabase側に登録するのは「アプリのURL（localhost/Pages）」。逆だと redirect_uri mismatch
- 作業環境：左にClaudeチャット、右にSupabaseの2画面並べて進行

### 次回の最短再開ルート
1. Authentication →「Sign In / Providers」→ Google → Callback URLコピー（C-1）
2. Google Cloud で OAuth client 作成・Client ID/Secret取得（C-2）
3. SupabaseのGoogleプロバイダに貼って有効化（C-3）
4. URL Configuration設定（C-4）
5. 以降：アプリ雛形実装（手順4）→ ローカル確認（手順5）→ GitHub Pages公開（手順6）→ keepalive（手順8）
